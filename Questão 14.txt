#include <stdio.h>

int main(void) {

float  x=0,y=0,z=0;


  printf("Digite x\n");

    scanf("%f",&x);

  printf("Digite y\n");

    scanf("%f",&y);

  printf("Digite z\n");

    scanf("%f",&z);

  if(x>y){
      if(y>z)
        printf("ordem crescente %f %f %f",z,y,x);

      else
        printf("ordem crescente %f %f %f",y,x,z);
  }

  if(y>x){
      if(x>z)
        printf("ordem crescente %f %f %f",z,x,y);

      else
        printf("ordem crescente %f %f %f",x,y,z);
  }

if(z>x){
      if(x>y)
        printf("ordem crescente %f %f %f ",y,x,z);

      else
        printf("ordem crescente %f %f %f ",x,y,z);
  }


  return 0;
}